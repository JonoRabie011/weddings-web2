{% set title = 'Gifts'%}
{% extends "base.twig" %}
{% block content %}
    <div id="carouselExampleSlidesOnly" class="carousel slide bg-light home-carousel gifts-carousel" data-bs-ride="carousel">
        <div class="gifts-carousel-overlay-img">
            <div class="gifts-carousel-overlay-img-headers position-absolute top-50 start-50 translate-middle">
                <h2 class="fancy-font gifts-carousel-overlay-img-headers-heading">
                    Gifts
                </h2>
                <p class="montserrat text-uppercase">
                    The most <span class="d-none d-md-inline fancy-font text-lowercase" style="
                    font-size: 62px !important; letter-spacing: 2px;">important gift</span>
                    <span class="d-md-none">
                        important
                        <span class="fancy-font text-lowercase" style="
                         font-size: 62px !important; letter-spacing: 2px;">
                            gift
                        </span>
                    </span>
                    to us is having you share in our special day.
                    <br class="d-none d-md-block"><br class="d-none d-md-block">
                    But if you wish to contribute in some
                    <span class="fancy-font text-lowercase" style="
                    font-size: 62px !important; letter-spacing: 2px;"> other way</span>, we would love
                    a few <span class="fancy-font text-lowercase" style="
                font-size: 62px !important; letter-spacing: 2px;"> pennies</span> to put in our pot, for our honeymoon trip after tying the <span class="fancy-font text-lowercase" style="
                font-size: 62px !important; letter-spacing: 2px;"> knot!</span>
                </p>
            </div>
        </div>
        <div class="carousel-inner">
            {% for img in data.carousel_imgs%}
                <div class="carousel-item {{loop.index == 1? 'active' : ''}}">
                    <div class="carousel-item-img d-none d-sm-block" style="background-image: url('{{img.standard}}')"></div>
                    <div class="carousel-item-img d-block d-sm-none" style="background-image: url('{{img.mobile}}')"></div>
                </div>
            {% endfor %}
        </div>

    </div>
{% endblock %}